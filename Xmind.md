# 任务板块
## 交互类
### 摇杆任务
1、摇杆数据采集
2、遥感数据处理
处理极性和范围、处理偏移问题、处理限幅
### 按键扫描任务
1、扫描按键 
2、处理按键数据
### OELD显示任务
定时刷新屏幕数据
## 通讯任务
数据来至摇杆任务和按键任务
1、打包遥控数据
帧头数据校验和
2、发送遥控数据
## 电源任务
定时激活按键避免关机


## 无线通讯模块
安信可公司有手册驱动电路等提供，需要对使用的芯片SI24R1进行驱动移植
使用芯片的SPI1外设，涉及引脚PB3(SPI1_SCK) PB4(SPI1_MISO) PB5(SPI1_MOSI) PA15(SPI_NSS) PB7 (SI_EN)-高电平有效配置时默认低电平
`READ_REG`、`WRITE_REG`修改驱动里这两个宏定义，因为在stm32库中会重复定义，修改为`SI24R1_WRITE_REG`、`SI24R1_READ_REG`。
剩下的宏定义不用修改，需要重写剩下的函数
## 按键扫描任务
一共六个按键
左上一个KEY_LEFT_X-PB2右上一个KEY_RIGHT_X-PB10，坐下手柄处4个按键KEY_UP-PB11,KEY_DOWN-PB14,KEY_LEFT-PB13,KEY_RIGHT-PB12
### 按键驱动
1、按键消抖处理
因为要在freertos中调用，所以用`vTaskDelay(10);`，延时来做消抖处理
2、避免按键多次判断
如果按键触发之后，消抖也完成了，就需要检测什么时候释放，用：
```c
     while (HAL_GPIO_ReadPin(KEY_UP_GPIO_Port, KEY_UP_Pin) == GPIO_PIN_RESET)
    {
         vTaskDelay(10);
    }
```
来处理，加`vTaskDelay(10);`避免人一直按着按键，导致低于按键任务的任务被饿死不得执行。
### 特殊按键
特殊按键在左上和右上，有长按短按的区分
按键任务处理长按与短按的思路：
1、检测按键是否按下，经过消抖确定按下之后，开始获取一个计数值，在freertos中可以使用`TickType_t start_tick = xTaskGetTickCount();`来获取一个初始计数值
2、然后开始循环等待按键的释放
3、按键释放之后再次获取一个计数值，并且和第一次获取的相减，获得按键按下多久`TickType_t duration = xTaskGetTickCount() - start_tick;`
4、判断`if (duration >= pdMS_TO_TICKS(1000))`大小
裸机不适合这个思路，裸机得用状态机判断，不然whlie循环等待按键释放哪就会一直占用cpu
## 摇杆任务
左边摇杆控制油门，顺时针逆时针旋转，右边摇杆控制前后左右得飞行
### ADC配置
1、ADC1-1 PA1 ADC1-2 PA2 ADC1-3 PA3 ADC1-6 PA6，两个摇杆ADC1-1 PA1、ADC1-6 PA6为一组剩下的为一组
2、打开ADC 1、2、3、6四个通道
3、打开ADC连续转换，连续转换就是一直扫描，如果不开启，那就触发一次ADC，ADC采集一次
4、转换通道配置，用到了4个配置4，CubeMX之中会自动打开扫描模式，就是对这四个通道依次采集
5、采样时间，采样电压时耗时时间，采样时间越长耗时越多但是越精准，可以旋转一个中间值
    公式计算：总转换时间 = (采样周期数 + 转换周期数) × (1 / ADC时钟频率)
    采样周期就是我们配置通道时设定的数，转换周期是固定的由分辨率决定
6、配置DMA直接数据传输，不需要经过CPU计算，直接用DMA将ADC读取到的值搬运到内存中去
DMA有循环模式和正常模式，循环模式：传输完指定数量后，自动重头开始，无限循环。只要外设触发，就会一直传输。正常模式：传输指定的数据量后，DMA 自动关闭，不再响应请求。若要再次传输，需软件重新使能。
配置循环模式，方向是寄存器到内存方向。
### 摇杆底层原理
1、摇杆数据复杂，用结构体存储
2、上位机使用VOFA监看摇杆值
注意我们输出的结构形式，可以使VOFA波形监控`debug_printf(":%d,%d,%d,%d\n", joystick_data.throttle, joystick_data.yaw, joystick_data.pitch, joystick_data.roll);`
## 合并按键任务和摇杆任务
也就是遥控器应用层采集数据处理
按键功能定义：
左上关机，右上短按定高和校准摇杆
按键的上下左右功能就是微调前后左右
遥控数据结构体定义：
```c
//遥控数据定义
typedef struct
{
    int16_t throttle;  //油门值，范围
    int16_t yaw;       //偏航值，范围
    int16_t pitch;     //俯仰值，范围
    int16_t roll;      //滚转值，范围
    int8_t altitude;  //定高值，范围
    int8_t shutdown;  //关机值，范围
} Remote_Data;
```
### 数据处理逻辑
对采集的ADC的值修改极性，零偏校准，按键微调，处理范围
零偏校准就是采集多次平均值后减去油门零偏值定义

